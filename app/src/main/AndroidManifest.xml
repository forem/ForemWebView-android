<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.forem.android">

  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.INTERNET" />

  <application
    android:name=".application.ForemApplication"
    android:allowBackup="false"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.Forem">
    <activity
      android:name=".presentation.explore.ExploreActivity"
      android:configChanges="orientation|screenSize|layoutDirection"
      android:label="@string/explore_activity_label" />
    <activity
      android:name=".presentation.home.HomeActivity"
      android:configChanges="orientation|screenSize|layoutDirection"
      android:exported="true"
      android:label="@string/home_activity_label">
      <intent-filter>
        <action android:name=".presentation.home.HomeActivity" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
    </activity>
    <activity
      android:name=".presentation.onboarding.OnboardingActivity"
      android:configChanges="orientation|screenSize|layoutDirection"
      android:label="@string/onboarding_activity_label" />
    <activity
      android:name=".presentation.splash.SplashActivity"
      android:configChanges="orientation|screenSize|layoutDirection"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <intent-filter>
        <data android:scheme="com.forem.app" />
        <action android:name="android.intent.action.SEND" />

        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />

        <data android:mimeType="text/plain" />
      </intent-filter>
    </activity>
    <activity
      android:name=".presentation.video.VideoPlayerActivity"
      android:configChanges="orientation|screenSize|layoutDirection" />

    <meta-data
      android:name="com.google.firebase.messaging.default_notification_color"
      android:resource="@color/forem_logo_color" />
    <meta-data
      android:name="com.google.firebase.messaging.default_notification_icon"
      android:resource="@drawable/ic_forem_notification" />

    <service android:name=".media.AudioService" />
    <service
      android:name=".presentation.MyFirebaseMessagingService"
      android:exported="false">
      <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
      </intent-filter>
    </service>
  </application>
</manifest>
